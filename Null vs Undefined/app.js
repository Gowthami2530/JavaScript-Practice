// Null vs Undefined
// Both represent no value

// Null - null will be  defined by the developer in the program
// Undefined - "javaScript can not defined any value to this."
// Variable without the value
// Missing function arguments
// Missing object properties

let number1 = 20 + null;  // 20 + 0 
console.log(number1);

let name1 = null;
console.log(name1);

let name2 = 30 + undefined
console.log(name2);

let number2; // there we have declared the variable and to that variable we didnot assigin the value so we get the result as undefine
console.log(number2);